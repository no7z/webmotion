# Spatial Product Document React adapter

Run `npm install --ignore-scripts` and `npm run dev`. Content and art direction live in `src/template.config.js`; the interaction contract lives at `../../contract.json`.
